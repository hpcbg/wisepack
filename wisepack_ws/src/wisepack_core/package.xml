<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>wisepack_core</name>
  <version>0.1.0</version>
  <description>WISEPACK domain model, deterministic task generator, packing algorithms, independent placement validator, KPI calculations and workflow engine. Deliberately free of any ROS dependency so the same logic runs with and without ROS 2.</description>
  <maintainer email="office@hpc.bg">High Performance Creators</maintainer>
  <license>MIT</license>

  <!-- No rclpy / std_msgs on purpose: this package must import on a machine
       with no ROS 2 installed, which is what the dashboard's sim mode needs. -->

  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
